---
name: github-explorer
description: >
  GitHub 项目深度分析 Skill。用于“帮我看看这个项目/值不值得用/分析一下仓库”类请求。
  先用 GitHub 主证据（repo metadata/issues/commits）建立结论，再按需补充外部信号，
  输出可追溯、可复查、可执行建议的结构化报告。强关联关键词：deepwiki、多源搜索、
  repo review、alternatives、竞品对比、项目健康度。支持“超详细深度调查”输出规格。
---

# GitHub Explorer — 项目深度分析

> Philosophy：README 只是门面，结论必须落在 Issues / Commits / 可追溯外链证据。

## 触发词（强触发）

出现以下任一表达，优先触发本 skill：

- “帮我看看这个仓库/这个项目怎么样/值不值得用”
- “做 repo review / 项目尽调 / 技术选型”
- “多源搜索验证这个项目”
- “看看有没有 DeepWiki / zread / arXiv 收录”
- “找这个项目的竞品/alternatives/对比”

## Workflow

```text
[项目名或 URL]
  -> [1. 定位仓库并消歧]
  -> [2. GitHub 主证据采集]
  -> [3. 外部补证 + 抓取降级]
  -> [4. 研判与结构化交付]
```

---

## 超详细模式（深度调查）

当用户出现“超详细/深度调查/深搜/尽调”字样时，按以下规格交付：

- 社区声量：至少 **6 条**
- 竞品对比：至少 **4 个**
- 反对证据：至少 **2 条**

若检索后不足配额：不得编造，必须写“未找到”并说明已尝试的查询方向。

---

## Phase 1：定位仓库并消歧（必须）

- 输入优先：完整 `owner/repo` 或 GitHub URL。
- 仅有项目名时，先用 `search-layer` 做资源定位：

```bash
uv run python "skills/search-layer/scripts/search.py" \
  "site:github.com <project_name>" --mode deep --intent resource --num 5
```

- 同名候选冲突必须写入 `notes`（为何选择该仓库）。
- Linux.do 锁帖/权限帖仅可作线索，不作为核心证据。

---

## Phase 2：GitHub 主证据采集（必须）

必须使用本仓脚本（内部含协议校验、报告渲染、产物落盘）：

```bash
uv run python "skills/github-explorer/scripts/explore.py" "owner/repo" --format markdown
```

深度模板：

```bash
uv run python "skills/github-explorer/scripts/explore.py" "owner/repo" \
  --issues 8 --commits 8 --external-num 10 --extract-top 3 \
  --confidence-profile deep --format markdown
```

参数边界（与校验器一致）：

- `--issues`: `3..20`
- `--commits`: `3..20`
- `--external-num`: `2..30`
- `--extract-top`: `0..external_num`
- `--format`: `json | markdown`

产物（默认）：

- 默认输出到 `".runtime/github-explorer/<repo>_<time>/"`
- 包含 `report.md`、`report.json`、`book/README.md`、可选论文 PDF
- 可用 `--out-dir` 指定目录，`--no-book-download` 关闭论文下载
- 脚本会做 DeepWiki、zread、arXiv 收录探测并写入报告（命中则给可点击链接，未命中给状态）
- 命中论文页（arXiv 或直接 PDF）时默认下载论文到 `book/papers/`

---

## Phase 3：外部补证 + 抓取降级（按需）

社区/竞品/替代方案：

```bash
uv run python "skills/search-layer/scripts/search.py" \
  --queries "<owner/repo> review" "<owner/repo> 使用体验" "<owner/repo> alternatives" \
  --mode deep --intent exploratory --num 5
```

时效追踪（必须 freshness）：

```bash
uv run python "skills/search-layer/scripts/search.py" \
  "<owner/repo> latest release" --mode deep --intent status --freshness pw --num 5
```

### search-layer 调用建议

| 场景 | 推荐命令 |
|---|---|
| 项目调研（默认） | `search.py --queries "<repo> review" "<repo> 使用体验" --mode deep --intent exploratory --num 5` |
| 最新动态 | `search.py "<repo> latest release" --mode deep --intent status --freshness pw --num 5` |
| 竞品对比 | `search.py --queries "<repo> vs <other>" "<repo> alternatives" --mode deep --intent comparison --num 5` |
| 快速查官方链接 | `search.py "<repo> docs" --mode fast --intent resource --num 3` |
| 深度调查（推荐） | `research.py "<repo> adoption risk and alternatives" --protocol codex_research_v1 --extract-per-round 2` |

默认说明：

- `deep` 走多源并行（Exa + Tavily + Grok）用于高覆盖补证。
- 证据冲突时必须标注冲突来源，不允许“合并成一句话带过”。
- 深度调查优先走 `research.py`（固定四轮），必要时可切 `legacy --max-rounds` 做补充轮。

### Extraction Upgrade（强制）

命中微信/知乎/小红书，或正文抽取失败/过短，必须升级到 `content-extract`：

```bash
uv run python "skills/content-extract/scripts/content_extract.py" --url "<url>"
```

必要时强制 MinerU：

```bash
uv run python "skills/content-extract/scripts/content_extract.py" --url "<url>" --force-mineru
```

---

## Phase 4：研判与输出（必须）

至少覆盖：

1. 项目阶段（快速迭代 / 稳定活跃 / 维护模式 / 低活跃）
2. 真实风险（Issue 暴露的问题 + maintainer 参与情况）
3. 适用边界（适合/不适合的团队与场景）
4. 采用建议（直接采用 / PoC 后采用 / 暂缓）

### 深度调查配额（强制）

- 社区声量：`>= 6` 条（至少覆盖 3 个平台）
- 竞品对比：`>= 4` 个（每个必须有可点击链接）
- 反对证据：`>= 2` 条（挑战项目优势的证据，需给来源）

### 精选 Issue 评分（建议）

每条 issue 可按 `0-10` 粗评分：

- 讨论热度（0-3）
- Maintainer 参与（0-3）
- 风险价值（0-2）
- 可执行性（0-2）

优先展示“高分 + 最近 90 天有进展”的议题。

---

## Output Contract（强制）

- 标题必须可点击：`# [owner/repo](https://github.com/owner/repo)`
- **L1（基础合同）**：markdown 版最少包含以下区块（与渲染器一致）：
  - `**🎯 一句话定位**`
  - `**📊 项目健康度**`
  - `**✅ 结果置信度**`
  - `**🔥 精选 Issue**`
  - `**🛠 最近提交**`
  - `**📰 外部信号**`
  - `**🧭 收录与索引**`
  - `**🆚 竞品对比**`
  - `**💬 我的判断**`
- **L2（增强交付，推荐）**：在 L1 基础上补充以下高信息密度区块
  - `**⚙️ 核心机制**`（不要复读 README）
  - `**✅ 适用场景**` / `**⚠️ 局限与风险**`
  - `**🌐 知识图谱**`（DeepWiki / zread / arXiv）
  - `**📰 社区声量**`（X/Reddit/V2EX/知乎等，必须具体引用）
- **L3（超详细深度调查）**：在 L2 基础上满足配额
  - `**📰 社区声量**` 至少 6 条
  - `**🆚 竞品对比**` 至少 4 个
  - `**❎ 反对证据**` 至少 2 条
- 所有外部观点必须带 URL；时间敏感信息带日期。
- 缺失项必须写“未找到”，禁止编造。
- 若 `explore.py` 返回码为 `2`（协议告警），必须补齐缺失区块后再交付。

模板参考：`references/report-template.md`

### 排版规则（强制）

1. 标题必须是仓库链接（可点击）。
2. 每个二级区块前后保持空行（避免挤在一起）。
3. 列表项后接下一个标题时，必须留空行。
4. 竞品与社区声量条目必须附链接。

### 输出模板（增强版，可直接复用）

```markdown
# [owner/repo](https://github.com/owner/repo)

**🎯 一句话定位**
{一句话说明项目做什么}

**⚙️ 核心机制**
- 技术路线：
- 关键模块：
- 设计取舍：

**📊 项目健康度**
- Stars/Forks/Open Issues/License
- 最近推送日期
- 阶段判断

**🔥 精选 Issue**
- [#123 title](url) — 核心争议 + maintainer 参与 + 风险标签

**🛠 最近提交**
- [`abc1234`](url) 变更摘要（YYYY-MM-DD）

**📰 外部信号**
- [来源标题](url)（YYYY-MM-DD）— 关键观点

**✅ 适用场景**
- 场景 1
- 场景 2

**⚠️ 局限与风险**
- 风险 1
- 风险 2

**🆚 竞品对比**
- vs [竞品A](url) — 差异
- vs [竞品B](url) — 差异
- vs [竞品C](url) — 差异
- vs [竞品D](url) — 差异

**🌐 知识图谱**
- DeepWiki: found/not_found/unavailable（链接）
- zread: found/not_found/unavailable（链接）
- arXiv: found/not_found/unavailable（链接）

**📰 社区声量**

**X / Reddit**
- [来源: 标题](url)（YYYY-MM-DD）— 具体观点（不要写“热度高”）
- [来源: 标题](url)（YYYY-MM-DD）— 具体观点
- [来源: 标题](url)（YYYY-MM-DD）— 具体观点

**中文社区**
- [知乎/V2EX/博客: 标题](url)（YYYY-MM-DD）— 具体观点
- [知乎/V2EX/博客: 标题](url)（YYYY-MM-DD）— 具体观点
- [知乎/V2EX/博客: 标题](url)（YYYY-MM-DD）— 具体观点

**❎ 反对证据**
- [来源标题](url)（YYYY-MM-DD）— 与项目主张相冲突的证据点
- [来源标题](url)（YYYY-MM-DD）— 风险/失败案例/关键质疑

**💬 我的判断**
- 建议：直接采用 / PoC 后采用 / 暂缓
- 下一步动作：验证项 A / 验证项 B
```

---

## Failure & Fallback

- 仓库无法解析：要求明确 `owner/repo`。
- 外部信号不足：保留 GitHub 主证据并在 `notes` 标注 `external_signal_limited`。
- 单源失败：继续其余来源，不阻塞流程。
- 外链抓取失败：切换 `content-extract` / `--force-mineru`。

---

## 输出前自检（强制）

- [ ] 仓库标题链接正确可点击
- [ ] 至少 3 条最近提交（不足需说明）
- [ ] 至少 1 条精选 Issue（无则写明）
- [ ] 外部信号均含 URL（无则写明）
- [ ] 社区声量 >= 6（不足需逐条写“未找到 + 已尝试查询方向”）
- [ ] 竞品对比 >= 4（不足需写“未找到”）
- [ ] 反对证据 >= 2（不足需写“未找到”）
- [ ] 结论含“适用边界 + 风险 + 建议动作”

---

## Dependencies

- `skills/search-layer`：外部检索与补证
- `skills/content-extract`：正文提取
- `skills/mineru-extract`：反爬兜底
- `src/codex_search_stack/github_explorer/`：主编排与报告渲染
